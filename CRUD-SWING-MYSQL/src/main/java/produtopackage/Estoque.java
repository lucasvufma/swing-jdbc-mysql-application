package produtopackage;

import java.util.List;

public class Estoque {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		SwingAPI teste = new SwingAPI();
/* Classe Teste apenas, já que decidi colocar a interface toda em classes separadas, não sei se isso foi uma boa ideia creio 
 * que acabou me dando muito mais trabalho, se eu tivesse trabalhado com ela na main muitas coisas teriam se resolvido
 * bem rapido, mas não sei se em uma aplicação de verdade o padrão a ser seguido é o de por a interface toda na main.  */
		
		
/*		Produto prod = new Produto("Farinha","Alimenticio",5.99,false);
		ProdutoDAO dao = new ProdutoDAO();
		dao.insertProduto(prod);
		List<Produto> p = dao.listProduto();
		for(Produto d : p) {
			System.out.println(d.toString());
		}   */
		
	}
}